# Python
.venv/
__pycache__/
*.pyc

# Traccia trace output (regenerated on each run)
traces.jsonl
traces_*.jsonl

# The replay sample trace IS required for the UI demo - keep it tracked.
!ui/replay/sample_run.jsonl

# Local logs
*.log

# Secrets - never commit a real key.
# Copy .env.example to .env and put your REAL key there. .env is ignored below,
# so it can never be committed by accident. .env.example (placeholder only) IS tracked.
.env
.env.local
.env.*.local


# Internal working files - never publish
PROJECT-STATUS.md
scripts/sanitize_replay.py
