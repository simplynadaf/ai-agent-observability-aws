# Traccia API key.
# Leave commented out to run in $0 local mode (traces write to traces.jsonl).
# Uncomment and paste your key from app.traccia.ai -> Settings -> API Keys to
# push the SAME spans to the hosted dashboard.
#TRACCIA_API_KEY=your_traccia_api_key_here
